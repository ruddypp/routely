# Routely Compose-First Replan: PM Canonical Plan

Task-based playbook: each checkbox task must be executable in a fresh agent context. This run is assigned to PM. Preserve unrelated dirty worktree changes. Commit only owned PM/doc/playbook changes after verification passes. User direction to encode: Routely must support both local and one-VPS operation; use a Compose-first deployment/runtime model inspired by Dokploy; normal setup and operation should be dashboard-first; bulk Start should start all enabled apps while each app can be stopped or disabled individually; include domain/proxy, env/secrets, database/backups, logs, deploy history, and health checks in the plan; UI should feel like Dokploy with the lightness/simplicity of 9router; target solo developers only, not teams/RBAC.

- [x] PM: rewrite the canonical product and alpha planning docs around the user's Compose-first Routely direction. Read `AGENTS.md`, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/02-team-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/04-docs-map.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, and `docs/12-prd.md` before editing. Update docs so the source of truth describes Routely as a dashboard-first solo-operator control plane that runs locally and on one VPS, starts all enabled Compose-backed apps together, supports per-app stop/disable, and plans the Dokploy-inspired surfaces: domain/proxy, env/secrets, database/backups, logs, deploy history, and health checks. Remove, rewrite, or explicitly defer legacy/non-relevant language such as teams/RBAC, multi-server, Kubernetes, marketplace/templates, broad cloud/managed-service positioning, and mock-success UI. Keep docs factual and implementation-aware; do not claim unimplemented behavior is complete. Run `git diff --check -- CONTEXT.md docs .maestro/playbooks` and targeted searches for stale terms: `rg -n "team|RBAC|multi-server|Kubernetes|marketplace|template|managed cloud|mock" docs README.md CONTEXT.md`. Commit only the PM-owned docs/playbook changes if verification passes.
  - Completed 2026-06-22 by PM. Canonical docs and README now frame Routely as a dashboard-first solo-operator, Compose-first local/one-VPS control plane with Start All, per-app stop/disable, and Dokploy-inspired domain/proxy, env/secrets, database/backups, logs, deploy history, and health surfaces. Verification passed with `git diff --check -- CONTEXT.md docs .maestro/playbooks` and the requested stale-term `rg`; remaining matches are role/workflow wording or explicit deferred/non-goal language.

- [x] PM: create executable Auto Run implementation playbooks for Backend and Frontend from the updated plan. Read the updated canonical docs, `docs/05-architecture.md`, `docs/06-interfaces.md`, and existing playbooks under `.maestro/playbooks/2026-06-21-Routely-Alpha-Execution/`. Create a new folder under `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/` with self-contained checkbox documents for Backend and Frontend only. Backend tasks must cover Compose-backed app model, app enablement/bulk start semantics, dashboard API contracts, local runner + one-VPS production parity, domain/proxy, env/secrets, database/backups, logs/deploy history, health checks, tests, and relevant lint/build checks. Frontend tasks must cover dashboard-first app creation/editing, start-all with per-app stop/disable, simple Dokploy-with-9router-taste information architecture, real-data status/log/deploy/domain/secrets/database/health screens, unsupported/deferred states, tests, and required Next.js docs reading before editing `apps/web`. Every implementation task must name files/modules to inspect, success criteria, verification commands, and commit rules. Run `git diff --check -- .maestro/playbooks docs CONTEXT.md` and commit only the PM-owned playbook/docs changes if verification passes.
  - Completed 2026-06-22 by PM. Created `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/BACKEND-01.md` and `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/FRONTEND-01.md` as task-based Auto Run docs. Verification passed with `git diff --check -- .maestro/playbooks docs CONTEXT.md`. No blockers.

- [x] PM: launch every Backend and Frontend Auto Run document created for the Compose-first execution plan, then write a launch manifest. Use `node /tmp/.mount_maestriig4I9/resources/maestro-cli.js list agents --json` to confirm the current Backend agent ID is `e2a663bf-0d9f-4cfb-9c03-3e49309356a5` and Frontend agent ID is `fbd438a5-13ce-40ef-93fa-6d867d6a5b95`. Maestro CLI 0.17.1 stores absolute Auto Run document paths, while the runner resolves them relative to `.maestro/playbooks`; before launching each generated doc, create the compatibility symlink path that the runner will read: for a doc `/home/ruddypp/Documents/work/routely/.maestro/playbooks/<folder>/<file>.md`, run `mkdir -p .maestro/playbooks/home/ruddypp/Documents/work/routely/.maestro/playbooks/<folder>` and `ln -sf /home/ruddypp/Documents/work/routely/.maestro/playbooks/<folder>/<file>.md .maestro/playbooks/home/ruddypp/Documents/work/routely/.maestro/playbooks/<folder>/<file>.md.md`. Then launch Backend docs with `node /tmp/.mount_maestriig4I9/resources/maestro-cli.js auto-run <backend-docs...> --launch --agent e2a663bf-0d9f-4cfb-9c03-3e49309356a5 --save-as "Routely Compose-First Backend Execution"`. Launch Frontend docs with `node /tmp/.mount_maestriig4I9/resources/maestro-cli.js auto-run <frontend-docs...> --launch --agent fbd438a5-13ce-40ef-93fa-6d867d6a5b95 --save-as "Routely Compose-First Frontend Execution"`. Write `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/LAUNCH-MANIFEST.md` listing exact docs launched, target agent IDs, command output, expected completion checks, the compatibility symlinks created, and how Routely Lead should verify all PM-created tasks were executed. Refresh Auto Run with `node /tmp/.mount_maestriig4I9/resources/maestro-cli.js refresh-auto-run`. Run `git diff --check -- .maestro/playbooks` and commit the launch manifest/playbook updates if verification passes.
  - Completed 2026-06-22 by PM. Confirmed Backend/Frontend agent IDs, created compatibility symlinks for `BACKEND-01.md` and `FRONTEND-01.md`, saved the named Backend/Frontend playbooks, launched each document without `--save-as` after CLI help showed `--save-as` is save-only, wrote `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/LAUNCH-MANIFEST.md`, and refreshed Auto Run. Verification passed with `git diff --check -- .maestro/playbooks`. No PM launch blockers.
