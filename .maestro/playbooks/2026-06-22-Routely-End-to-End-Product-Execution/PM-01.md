# Routely End-to-End PM Plan

- [ ] PM: turn the user's clarified product concept into a role-ready PRD and execution plan. Read `AGENTS.md`, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/02-team-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, and `.maestro/playbooks/2026-06-22-Routely-End-to-End-Product-Execution/EXECUTION-MANIFEST.md` before writing. Update docs only where the product north star is clearer: Routely is a single-command, local-or-one-VPS, solo-operator control plane/proxy for many reasonable app stacks; UX should feel lightweight like 9Router while operational scope follows the useful solo-developer parts of Dokploy. Produce a structured PRD or execution spec under `docs/` that defines user stories, vertical slices, acceptance criteria, out-of-scope boundaries, role handoffs, and verification gates. Do not publish GitHub issues until the user/Lead approves the slice breakdown. Run `git diff --check -- docs .maestro/playbooks` and commit only PM-owned docs/playbooks if checks pass.

- [ ] PM: convert the approved PRD into independently grabbable vertical-slice issues or Maestro playbooks. Each slice must be demoable end-to-end and cite its owner roles: single-command local runner, dashboard app registry, Start All/per-app lifecycle, local proxy/routes, stack templates, one-VPS runtime, production operations, GitHub redeploy, and release readiness. Use `ready-for-agent` labels if publishing GitHub issues. Keep QA/Security report ownership with Lead, not QA/Security commits.
