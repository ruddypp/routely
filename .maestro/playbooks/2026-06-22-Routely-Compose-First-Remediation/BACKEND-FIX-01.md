# Routely Compose-First Backend Remediation

Task-based playbook: each checkbox task must be executable in a fresh Backend agent context. Backend owns `apps/cli`, `apps/daemon`, `packages/*`, backend tests, and backend behavior docs for touched surfaces. Preserve unrelated dirty worktree changes. Commit only Backend-owned files changed for the current task after verification passes. Do not edit `apps/web` except when explicitly instructed by Routely Lead.

- [ ] Backend: remediate backend-owned production auth contract gaps for `SEC-COMPOSE-01`. Read `AGENTS.md`, `docs/02-team-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `docs/security/compose-first-trust-boundary-2026-06-22.md`, `apps/daemon/src/server.js`, `apps/cli/src/index.ts`, and backend auth tests before editing. Preserve daemon production admin-token enforcement and define one canonical backend production-mode signal that Frontend route handlers can rely on without leaking secrets; if the current daemon/auth status contract already exposes this safely, add regression tests instead of broad rewrites. Cover the security report reproduction where the daemon is production-protected but dashboard env can be misconfigured. Success criteria: backend production mode/auth state remains explicit and testable, unauthenticated production daemon mutations still return 401, and any docs/config examples touched by Backend name the canonical production env contract. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, auth-focused targeted tests such as `npm exec --workspace apps/cli -- vitest run src/checkpoint12.test.ts` if relevant, `node --check apps/daemon/src/server.js`, and `git diff --check -- apps packages docs` if docs change. Commit only this task's Backend-owned files with a `MAESTRO: ` prefixed message if verification passes.

- [x] Backend: fix URL/DSN/URI env secret classification and redaction for `SEC-COMPOSE-02`. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `docs/security/compose-first-trust-boundary-2026-06-22.md`, `packages/core/src/index.js`, `packages/core/src/index.d.ts`, `packages/db/src/index.js`, `apps/daemon/src/server.js`, and existing env/log tests before editing. Treat common connection-string keys as secret by default, including at least `DATABASE_URL`, `REDIS_URL`, `MONGODB_URI`, `POSTGRES_URL`, `MYSQL_URL`, `SENTRY_DSN`, `WEBHOOK_URL`, and provider URL/DSN/URI variants; prevent raw values from public DTOs returned by `/apps`, `/apps/:id/env`, logs, deployment logs, and config-exportable env surfaces unless there is an explicit safe-display contract. Include secret-like values from `routely.yml` app env in redaction where practical, not only rows stored in `app_env_vars`. Add focused tests for the reproduction command in the Security report and for URL-shaped secret values in app/env/log/deployment DTO paths. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, targeted env/log tests such as `npm exec --workspace apps/cli -- vitest run src/checkpoint8.test.ts src/checkpoint10.test.ts src/checkpoint12.test.ts`, `node --check apps/daemon/src/server.js`, and `npm run build --workspace apps/cli` if CLI output changes. Commit only this task's Backend-owned files with a `MAESTRO: ` prefixed message if verification passes.
  - Completion note (Backend, 2026-06-22): Env-secret redaction changes are committed through `71a3d62` with URL/DSN/URI secret-key inference, public env DTO/config filtering, legacy row redaction, and `routely.yml` app env values included in app/deployment log redaction. SEC-COMPOSE-02 verification passed with `npm exec --workspace apps/cli -- vitest run src/checkpoint8.test.ts src/checkpoint10.test.ts --reporter verbose`, `npm exec --workspace apps/cli -- vitest run src/checkpoint12.test.ts -t "redacts saved app secrets|redacts URL-shaped secrets|redacts database app env values|redacts config env secrets" --reporter verbose`, `npm run lint`, `node --check apps/daemon/src/server.js`, and `git diff --check -- apps packages docs`. Full `npm run test --workspace apps/cli` currently fails on unrelated concurrent SEC-COMPOSE-04 health/backup path-redaction work (`src/checkpoint10.test.ts` backup `filePath` expectation and `src/checkpoint12.test.ts` production `/health` path-exposure assertion), not on the SEC-COMPOSE-02 env/log cases.

- [ ] Backend: harden notification outbound URL handling for `SEC-COMPOSE-03`. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/07-security-and-risks.md`, `docs/security/compose-first-trust-boundary-2026-06-22.md`, notification DTO/helpers in `packages/core/src/index.js`, notification persistence in `packages/db/src/index.js`, and notification delivery code in `apps/daemon/src/server.js` before editing. Change notification fetch behavior so redirects are not followed unless each redirect target passes the same safe outbound URL policy; resolve and validate hostnames as close to connect time as the current runtime allows, reject loopback/private/link-local/localhost-equivalent targets including IPv4-mapped IPv6, and classify failed webhook response snippets as untrusted external text without exposing excessive body content. Add tests for redirect-to-loopback, unsafe redirect chains, localhost-equivalent hosts, DNS-rebinding simulation where feasible, and safe public webhook delivery. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, targeted notification/security tests such as `npm exec --workspace apps/cli -- vitest run src/checkpoint12.test.ts`, `node --check apps/daemon/src/server.js`, and `git diff --check -- apps packages`. Commit only this task's Backend-owned files with a `MAESTRO: ` prefixed message if verification passes.

- [ ] Backend: remove unnecessary public path metadata from health and backup DTOs for `SEC-COMPOSE-04` and backend-owned `SEC-COMPOSE-05`. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `docs/security/compose-first-trust-boundary-2026-06-22.md`, health/server routes in `apps/daemon/src/server.js`, backup DTO helpers in `packages/core/src/index.js`, backup persistence in `packages/db/src/index.js`, and existing health/backup tests before editing. Split unauthenticated production liveness from authenticated diagnostic health so public `/health` does not expose absolute `workspace`, `database`, data directory, or other filesystem paths. Change public backup DTOs to expose file name, size, status, storage class, `downloadUrl: null`, `servesFile: false`, and restore-deferred state by default while keeping absolute local paths only for authenticated diagnostic CLI/admin-only surfaces if needed. Add regression tests for unauthenticated production `/health` path redaction and `/backups` path redaction. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, targeted health/backup tests such as `npm exec --workspace apps/cli -- vitest run src/checkpoint9.test.ts src/checkpoint12.test.ts`, `node --check apps/daemon/src/server.js`, and `npm run build --workspace apps/cli` if CLI/admin output changes. Commit only this task's Backend-owned files with a `MAESTRO: ` prefixed message if verification passes.

- [ ] Backend: decide and implement or explicitly document health/metrics retention for `SEC-COMPOSE-06`. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `docs/09-current-status.md`, `docs/security/compose-first-trust-boundary-2026-06-22.md`, metric/health repositories in `packages/db/src/index.js`, metric/health routes in `apps/daemon/src/server.js`, and existing metrics/health tests before editing. Prefer a small alpha retention policy that prunes old health and metrics samples during writes or scheduled reads; if implementation risk is too high for this remediation pass, document the exact alpha retention behavior in Backend-owned docs and mark it as accepted until after public alpha. Do not let UI/API copy imply “latest 30 displayed” means “only 30 retained” unless retention is implemented. Add tests for whichever policy is chosen. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, targeted metrics/health tests such as `npm exec --workspace apps/cli -- vitest run src/checkpoint10.test.ts src/checkpoint12.test.ts`, `node --check apps/daemon/src/server.js`, and `git diff --check -- apps packages docs`. Commit only this task's Backend-owned files with a `MAESTRO: ` prefixed message if verification passes.

- [ ] Backend: run final backend remediation verification for `SEC-COMPOSE-01`, `SEC-COMPOSE-02`, `SEC-COMPOSE-03`, `SEC-COMPOSE-04`, `SEC-COMPOSE-05`, and `SEC-COMPOSE-06`. Read the commits and files changed by the previous Backend remediation tasks, `docs/security/compose-first-trust-boundary-2026-06-22.md`, and `docs/qa/compose-first-e2e-validation-2026-06-22.md` before running checks. Do not broaden scope or rewrite unrelated code. Run `npm run lint`, `npm run test --workspace apps/cli`, `npm run build --workspace apps/cli`, `node --check apps/daemon/src/server.js`, and targeted security regression tests added during this remediation. If all checks pass and this task changes Backend-owned docs or tests, commit only those files with a `MAESTRO: ` prefixed message. If any check fails, fix current-task regressions; report unrelated failures with exact command output in the Auto Run result.
