---
type: reference
title: Routely Compose-First Frontend Remediation Playbook
created: 2026-06-22
tags:
  - maestro-playbook
  - frontend
  - compose-first
  - remediation
  - public-alpha
related:
  - '[[Routely Compose-First E2E QA Validation]]'
  - '[[Compose-First Trust-Boundary Security Validation]]'
  - '[[Routely Compose-First Remediation Manifest]]'
---

# Routely Compose-First Frontend Remediation

Task-based playbook: each checkbox task must be executable in a fresh Frontend agent context. Preserve unrelated dirty worktree changes. Frontend owns `apps/web`, route-handler tests, frontend UI behavior, and frontend docs for touched dashboard surfaces. Read relevant Next.js docs under `node_modules/next/dist/docs/` before changing Next.js code because this project uses a breaking-change Next.js version. Commit only Frontend-owned files for the current task after verification passes. QA E2E and Security do not commit their own reports; those reports are handed to Routely Lead for review/commit.

- [x] Frontend: fix responsive dashboard clipping for `FE-QA-2026-06-22-01` from `docs/qa/compose-first-e2e-validation-2026-06-22.md`. Read `AGENTS.md`, the relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, `apps/web/src/app/dashboard-client.tsx`, dashboard shell/header components, app row/card components, existing dashboard smoke code, and the QA screenshots at `.maestro/playbooks/Working/qa-compose-e2e-smoke/screenshots/dashboard-smoke-375x812.png`, `.maestro/playbooks/Working/qa-compose-e2e-smoke/screenshots/dashboard-smoke-768x1024.png`, and `.maestro/playbooks/Working/qa-compose-e2e-smoke/screenshots/dashboard-smoke-1280x800.png` before editing. Audit fixed-width grids, missing `min-w-0`, nowrap action bars, badges/status pills, row metadata columns, and table/card layouts that create horizontal overflow or text/action overlap. Stack or wrap app row metadata/actions on mobile, preserve readable status/header controls, keep `Start All`, `Add resource`, and app action buttons reachable at 375px, and ensure desktop/tablet rows do not overlap. Add or update smoke assertions so `document.documentElement.scrollWidth <= window.innerWidth` at 375px, 768px, and 1280px, and capture updated screenshots when the smoke command supports it. Success criteria: responsive dashboard acceptance is no longer blocked by clipped primary content or off-screen controls. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, focused dashboard/component tests for changed modules, `ROUTELY_DASHBOARD_URL=http://127.0.0.1:13030 ROUTELY_DASHBOARD_SMOKE_DIR=/home/ruddypp/Documents/work/routely/.maestro/playbooks/Working/qa-compose-e2e-remediation-screenshots npm run test:dashboard-smoke --workspace apps/web` when a seeded dashboard is available, and `git diff --check -- apps/web docs .maestro/playbooks`. Commit only this task's Frontend-owned files after checks pass; QA/Security verification reports remain uncommitted by QA/Security.

  Notes:
  - Implemented responsive layout remediation in `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/components/dashboard/shell.tsx`, `apps/web/src/components/dashboard/top-status-bar.tsx`, and `apps/web/src/components/dashboard/module-header.tsx`.
  - Updated `apps/web/scripts/dashboard-smoke.mjs` to fail if document/body scroll width exceeds the viewport at the configured 375px, 768px, or 1280px smoke sizes.
  - Verification passed: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, `npm run build --workspace apps/web`, and `git diff --check -- apps/web docs .maestro/playbooks`.
  - Smoke screenshot capture was blocked because `http://127.0.0.1:13030` refused connections; QA/Lead should rerun with seeded dashboard state to collect updated screenshots.


- [ ] Frontend: enforce dashboard route-handler caller auth for frontend-owned `SEC-COMPOSE-01`. Read `AGENTS.md`, relevant Next.js route-handler docs under `node_modules/next/dist/docs/`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `apps/web/src/lib/daemon.ts`, all route handlers under `apps/web/src/app/api/`, existing auth/route tests, `.env.example`, and `README.md` before editing. Require dashboard caller auth whenever any production signal is true: `ROUTELY_ENV=production`, `ROUTELY_SERVER_MODE=production`, persisted production state if exposed by Backend, or `ROUTELY_ADMIN_TOKEN` exists. Default-deny all mutating route handlers when an admin token exists unless the caller provides the matching bearer/admin token, while preserving local no-token dashboard behavior for truly local development. Add route-handler tests for `ROUTELY_SERVER_MODE=production` with `ROUTELY_ENV` unset, `ROUTELY_ADMIN_TOKEN` set with no caller token, authorized caller token success, and high-impact mutation paths such as deployments, env, backups, notifications, domains/proxy, and GitHub operations. Success criteria: a network-exposed dashboard cannot proxy its server-side daemon token to protected daemon mutations without caller auth. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, targeted auth/route tests, and `git diff --check -- apps/web docs README.md .env.example`. Commit only this task's Frontend-owned files after checks pass; QA/Security verification reports remain uncommitted by QA/Security.

- [ ] Frontend: verify URL/DSN env redaction display for frontend-owned `SEC-COMPOSE-02` without treating `safeEnvDisplay` as the primary control. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/07-security-and-risks.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/dashboard-operations.ts`, `apps/web/src/lib/daemon.ts`, `/api/apps` and `/api/apps/:id/env` route handlers, and existing env/secrets tests before editing. Keep UI rendering defensive for `DATABASE_URL`, `REDIS_URL`, `MONGODB_URI`, `POSTGRES_URL`, `MYSQL_URL`, `SENTRY_DSN`, `WEBHOOK_URL`, and credentialed URL-like values even if an older daemon returns raw values; show redacted metadata, restart/redeploy-needed state, and safe labels rather than raw values. Add tests proving dashboard rendering and web API responses do not expose raw URL/DSN secrets once Backend DTOs are fixed, and that any legacy fallback display is still masked. Success criteria: raw secret-like URL/DSN values do not appear in dashboard text, snapshots, route responses, or screenshots. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, focused env/secrets route/UI tests, and `git diff --check -- apps/web docs`. Commit only this task's Frontend-owned files after checks pass; QA/Security verification reports remain uncommitted by QA/Security.

- [ ] Frontend: remove absolute backup path rendering for frontend-owned `SEC-COMPOSE-05` display remediation. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/07-security-and-risks.md`, `docs/09-current-status.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/dashboard-operations.ts`, backup route handlers under `apps/web/src/app/api/backups/`, and existing backup UI/route tests before editing. Prefer rendering `fileName`, size, status, storage class, `downloadUrl: null`, `servesFile: false`, and `restoreStatus: deferred`; do not render `localDir`, `filePath`, or nested absolute `path` values in normal dashboard views even if an older daemon includes them. Keep alpha copy honest that backup files are local metadata-only and destructive restore automation is deferred. Add tests with custom `localDir`/absolute path fixtures proving dashboard text and web route DTOs do not display full host paths. Success criteria: screenshots/support logs from the backup dashboard no longer leak host filesystem layout while still explaining storage and restore limitations. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, focused backup route/UI tests, and `git diff --check -- apps/web docs`. Commit only this task's Frontend-owned files after checks pass; QA/Security verification reports remain uncommitted by QA/Security.

- [ ] Frontend: run an integrated frontend remediation verification pass after the preceding Frontend fixes for `FE-QA-2026-06-22-01`, frontend-owned `SEC-COMPOSE-01`, frontend-owned `SEC-COMPOSE-02`, and frontend-owned `SEC-COMPOSE-05`. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, changed `apps/web` files from this remediation folder, and current git history before running checks. Do not broaden scope or rewrite unrelated code. Run `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, targeted route/UI tests touched by the fixes, and the dashboard smoke command at 375px/768px/1280px when a seeded dashboard and token configuration are available. If real dashboard smoke is blocked by missing seeded state, daemon token mismatch, or unavailable credentials/endpoints, record the exact blocker and owner in the Auto Run output rather than marking product acceptance complete. Success criteria: frontend-owned blockers are fixed or explicitly reported with exact remaining environment blockers; no raw secrets/paths render; no viewport overflow remains. Commit only Frontend-owned files if this task creates changes and all relevant checks pass; QA/Security verification reports remain uncommitted by QA/Security.
