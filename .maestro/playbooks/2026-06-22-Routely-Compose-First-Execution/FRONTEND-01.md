# Routely Compose-First Frontend Execution

Task-based playbook: each checkbox task must be executable in a fresh Frontend agent context. Preserve unrelated dirty worktree changes. Frontend owns `apps/web`, same-origin web route handlers, frontend tests, dashboard state integration, and frontend docs for touched surfaces. Before editing `apps/web`, always follow `AGENTS.md` and read the relevant Next.js guide under `node_modules/next/dist/docs/`. Commit only files changed for the current task after verification passes.

- [x] Frontend: implement or verify dashboard-first app creation/editing for the Compose-first registry. Read `AGENTS.md`, the relevant Next.js docs under `node_modules/next/dist/docs/` for App Router route handlers/client components, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/daemon.ts`, and the route handlers under `apps/web/src/app/api/apps/` before editing. Ensure the dashboard app form and API integration can represent name, type, path/source, command or Compose metadata, port, dependencies, healthcheck, domains, non-secret env metadata, and enablement state. Keep unsupported production-only fields hidden, disabled, or explicitly marked deferred. Success criteria: create/edit flows use real same-origin `/api/*` data, preserve enablement and Compose fields already supported by backend DTOs, and do not show mock success for unsupported drivers/actions. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and targeted route-handler/UI tests for changed code. Commit only this task's files with a concise Frontend message if verification passes.

  - Completed 2026-06-22 by Frontend: extracted and hardened dashboard app registry form serialization, preserved redacted env metadata on edits, preserved GitHub auto-deploy source metadata, kept Compose fields/enabled state in same-origin `/api/apps` create/edit payloads, and added focused form/route-handler coverage.
  - Follow-up hardening 2026-06-22 by Frontend: made command vs Compose fields driver-scoped in the form, labeled domains/source as registry intent, added validation for incomplete command/Compose/source metadata, and aligned web daemon types with public app DTO fields.
  - Verification passed: `npm run lint`; `npm run test --workspace apps/web`; `npx tsc --noEmit --project apps/web/tsconfig.json`; `npm run build --workspace apps/web` (captured exit `0`).

- [x] Frontend: add or verify Start All, per-app stop, and enable/disable dashboard states. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/daemon.ts`, app lifecycle route handlers under `apps/web/src/app/api/apps/`, and existing web tests before editing. Implement the dashboard's Start All affordance only if a backend/same-origin API contract exists; otherwise show an explicit deferred state that points to the current verified CLI fallback. Per-app stop must be distinct from disabling an app, and disabled apps must remain visible with a clear skipped-from-Start-All reason. Success criteria: enabled apps can be started where implemented, disabled apps are not presented as startable, one running app can be stopped without implying the rest will stop, and all disabled/deferred reasons are visible and accessible. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and focused tests for app lifecycle UI/route state. Commit only this task's files if verification passes.
  - Notes: Added the same-origin `/api/apps/start-all` route for the daemon bulk-start contract, wired the dashboard Start All button to real `/api/*` data, kept disabled resources visible with skipped-from-Start-All labels, disabled unsupported driver lifecycle actions as deferred, and preserved per-app stop as a runtime action that does not toggle enablement.
  - Verification: `npm run test --workspace apps/web -- app-start-route.test.ts app-lifecycle.test.ts`, `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, `git diff --check`, and `npm run build --workspace apps/web` passed.

- [x] Frontend: reshape the dashboard information architecture toward Dokploy-inspired operations with 9Router-like lightness. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/11-feature-scope.md`, `apps/web/src/app/dashboard-client.tsx`, any local component/style files in `apps/web/src`, and existing dashboard tests before editing. Keep the dashboard dense and operational rather than marketing-like: overview, apps/services, deployments, domains, GitHub, env/secrets, logs, health, metrics, databases, backups, notifications/settings, and server status should be easy to scan without enterprise/team/RBAC affordances. Success criteria: navigation and section copy emphasize solo-operator local-to-one-VPS operation, unsupported states are labeled `deferred` or disabled rather than hidden behind success styling, and the UI avoids fake readiness/magic success. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and screenshot/Playwright smoke only if an existing project command supports it. Commit only this task's files if verification passes.
  - Notes: Reworked dashboard nav groups to Operate/Release/Observe/Data/Node, added Server Status as a first-class module, tightened solo local-to-one-VPS copy, and surfaced deferred release/GitHub/settings capabilities explicitly.
  - Verification: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and `git diff --check` passed. `npm run test:dashboard-smoke --workspace apps/web` was attempted but the current running dashboard is in production-auth mode and `/api/apps` returns `Routely production API requires an admin token.`, with no registered seed apps, so the smoke script could not satisfy its existing `hello-*` app-row assertion.

- [ ] Frontend: make production operations screens real-data backed for deploy, domain/proxy, env/secrets, database/backups, logs, deploy history, and health. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/daemon.ts`, and route handlers under `apps/web/src/app/api/deployments/`, `apps/web/src/app/api/domains/`, `apps/web/src/app/api/proxy/`, `apps/web/src/app/api/apps/[id]/env/`, `apps/web/src/app/api/databases/`, `apps/web/src/app/api/backups/`, and `apps/web/src/app/api/apps/[id]/health/` before editing. Ensure these screens distinguish generated route, DNS mismatch, pending TLS, verified TLS, failed TLS, missing auth, deploy in progress, failed phase, latest successful deployment, redacted secret metadata, internal database, backup job/run status, log availability, and health state where backend data exists. Success criteria: every demo-critical production module renders real `/api/*` data or an honest unavailable/deferred/error state; raw secrets are never rendered; generated config is not shown as certificate success. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and focused route/UI tests for changed modules. Commit only this task's files if verification passes.

- [ ] Frontend: harden GitHub redeploy diagnosis and unsupported/deferred states. Read `AGENTS.md`, relevant Next.js docs under `node_modules/next/dist/docs/`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/lib/daemon.ts`, route handlers under `apps/web/src/app/api/github/`, and existing GitHub/deploy tests before editing. Show GitHub connection status, configured repo/branch, latest delivery, latest deploy, ignored/failing events, commit metadata where available, failure phase, deploy history, and path to deployment logs. Avoid implying preview deployments, live repo browsing, commit status updates, teams/RBAC, or multi-server behavior exist. Success criteria: broken GitHub deploys are diagnosable from dashboard state; untrusted GitHub metadata is rendered safely as text; unsupported future features are disabled/hidden/deferred. Verification commands: `npm run lint`, `npm run test --workspace apps/web`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and focused GitHub route/UI tests. Commit only this task's files if verification passes.

- [ ] Frontend: run a final web verification pass for Compose-first dashboard changes and document blockers. Read the current git history from this playbook run, `docs/03-demo-acceptance-plan.md`, and the `apps/web` files changed by previous Frontend tasks in this execution folder. Do not broaden scope or rewrite unrelated code. Run `npm run lint`, `npm run test --workspace apps/web`, and `npx tsc --noEmit --project apps/web/tsconfig.json`; if UI behavior materially changed and an existing Playwright or screenshot command is available, run the narrowest relevant smoke. If all checks pass and there are frontend-owned changes from this task, commit only those files with a concise Frontend message. If checks fail, determine whether each failure is caused by current Frontend changes or an unrelated existing issue; fix current-task failures, but report unrelated blockers with exact command output in the Auto Run task output.
