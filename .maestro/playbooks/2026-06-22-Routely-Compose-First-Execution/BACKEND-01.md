# Routely Compose-First Backend Execution

Task-based playbook: each checkbox task must be executable in a fresh Backend agent context. Preserve unrelated dirty worktree changes. Backend owns `apps/cli`, `apps/daemon`, `packages/*`, backend tests, and backend behavior docs for touched surfaces. Commit only files changed for the current task after verification passes. Do not edit `apps/web` except when explicitly instructed by Routely Lead.

- [x] Backend: harden the app registry, Compose metadata, and enablement contract. Read `AGENTS.md`, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, `packages/core/src/index.js`, `packages/core/src/index.d.ts`, `packages/db/src/index.js`, `packages/db/src/index.d.ts`, `apps/daemon/src/server.js`, and `apps/cli/src/index.ts` before editing. Ensure `enabled`, `driver`, `compose_file`, `compose_service`, dependencies, ports, healthcheck, domains, source, and non-secret env metadata round-trip through config normalization, SQLite persistence, daemon DTOs, and CLI/app listing output. Disabled apps must remain registered and visible but must be excluded from bulk start semantics. Add or update focused tests near existing core/DB/CLI tests for config normalization, DTO mapping, DB persistence, and disabled-app filtering. Success criteria: a disabled app is preserved in config/state/API/CLI output, enabled `command` and `compose` resources still start locally, and unsupported fields are not silently dropped. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, any targeted package tests for changed files, and `node --check apps/daemon/src/server.js` if daemon code changes. Commit only this task's files with a concise Backend message if verification passes; if a required check fails for an unrelated reason, report the exact blocker in the Auto Run output and do not hide it in the commit message.
  - Notes: Backend hardened registry normalization, SQLite persistence, daemon/CLI DTO output, and focused CLI coverage for disabled Compose-backed resources, ports, service/database metadata, and secret-like env redaction. Verification passed: `npm run test --workspace apps/cli`, `npm run build --workspace apps/cli`, `npm run lint`, `node --check apps/daemon/src/server.js`.

- [x] Backend: implement or verify dashboard lifecycle API contracts for Start All, per-app stop, and enable/disable. Read `AGENTS.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `apps/daemon/src/server.js`, `apps/cli/src/index.ts`, `packages/core/src/index.js`, `packages/db/src/index.js`, and existing tests for app start/stop routes before editing. Provide backend contracts that allow the dashboard to: start all enabled local `command` and `compose` resources in dependency order; report skipped disabled apps; stop one running app without changing enablement; toggle enablement without deleting config/history; and return actionable error reasons for daemon offline, unsupported driver, disabled app, already running, not running, dependency cycle, and port conflict states. If a full Start All daemon route already exists, verify it and add missing tests; if not, add the narrowest daemon/API contract needed for Frontend to call through same-origin `/api/*` route handlers. Success criteria: lifecycle behavior is externally visible through daemon responses and/or CLI output, disabled resources are skipped rather than started, and per-app stop does not change `enabled`. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, targeted daemon/API tests for changed routes, and `node --check apps/daemon/src/server.js` if daemon code changes. Commit only this task's files if verification passes.
  - Notes: Backend added daemon `POST /apps/start-all` with dependency-ordered local `command`/`compose` starts, skipped disabled/unsupported/already-running resources, duplicate/unavailable port conflict reporting, and dependency-cycle errors. Per-app lifecycle errors now include stable `code` values for disabled, unsupported driver, already running, not running, port conflict, and start/stop failure states; stop keeps `enabled` unchanged and PATCH enablement remains non-destructive. Focused daemon integration coverage was added in `apps/cli/src/checkpoint12.test.ts`. Verification passed: `npm run lint`, `npm run test --workspace apps/cli`, targeted lifecycle coverage via `npm run test --workspace apps/cli -- src/checkpoint12.test.ts`, and `node --check apps/daemon/src/server.js`.

- [ ] Backend: harden local runner and one-VPS production parity around Compose-backed app operation. Read `AGENTS.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, `packages/drivers/src/index.js`, `packages/drivers/src/index.d.ts`, `apps/cli/src/index.ts`, `apps/daemon/src/server.js`, and deployment-related DB code before editing. Verify local Compose services/databases still start with Docker Compose, then move the production deploy path toward the documented Compose-backed target without overstating unimplemented behavior. If production Compose parity is feasible within this task, implement the smallest working path for one Compose-backed app on one VPS; if not, keep Dockerfile deploy behavior working and expose clear bridge/deferred state for production Compose. Success criteria: current Dockerfile deploy tests still pass, Compose-backed local resources are not regressed, production deploy records/logs remain useful, and any unsupported production Compose path returns an honest deferred/error state rather than mock success. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, driver/deploy-focused tests you add or update, `npm run build --workspace apps/cli`, and `node --check apps/daemon/src/server.js` if daemon code changes. Commit only this task's files if verification passes.

- [x] Backend: harden domain/proxy, env/secrets, and production auth contracts for one-VPS operation. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `packages/proxy/src/index.js`, `packages/proxy/src/index.d.ts`, env/secrets helpers in `packages/core` and `packages/db`, `apps/daemon/src/server.js`, and relevant CLI command code before editing. Ensure domain add/verify behavior is honest, DNS verification uses the configured server public IP where possible, generated Traefik-compatible routes target the latest successful deployment where available, TLS state does not claim certificate success from config generation alone, production mutation APIs require admin auth, secret values are redacted after save, and restart/redeploy-needed state is represented where implemented. Success criteria: domain/proxy responses distinguish generated, pending, verified, failed, and not-configured states; raw secrets do not appear in public DTOs/log output; unauthenticated production mutations are rejected. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, proxy/env/auth-focused tests for changed code, and `node --check apps/daemon/src/server.js` if daemon code changes. Commit only this task's files if verification passes.
  - Completion note: Domain/proxy DTOs now expose honest DNS/proxy/TLS states, generated routes point at the latest successful deployment, app env secrets stay redacted in public responses, production mutation paths reject unauthenticated requests, and env pending restart/redeploy state is covered. Verified with `npm exec --workspace apps/cli -- vitest run src/checkpoint6.test.ts src/checkpoint8.test.ts src/checkpoint12.test.ts`, `npm run lint`, `npm run test --workspace apps/cli`, `node --check apps/daemon/src/server.js`, and `npm run build --workspace apps/cli`.

- [x] Backend: harden databases/backups, logs/deploy history, and health checks for demo diagnosis. Read `AGENTS.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, database/backup repositories in `packages/db/src/index.js`, database presets in `packages/presets/src/index.js`, driver/log helpers in `packages/drivers/src/index.js`, health/metrics/deployment routes in `apps/daemon/src/server.js`, and relevant CLI output in `apps/cli/src/index.ts` before editing. Ensure production database records are internal-only by default, backup jobs/runs expose status and local-file metadata without serving sensitive files, retention behavior is honest where implemented, deployment phases/log links/history remain inspectable, failed deploys expose phase and logs, and healthcheck state supports dashboard diagnosis. Success criteria: database/backup DTOs do not leak secrets or imply unsupported restore/external storage; deploy history includes useful failure metadata; health endpoints return real state or honest unavailable/deferred state. Verification commands: `npm run lint`, `npm run test --workspace apps/cli`, database/backup/deploy/health targeted tests for changed files, `npm run build --workspace apps/cli`, and `node --check apps/daemon/src/server.js` if daemon code changes. Commit only this task's files if verification passes.
  - Completion note: Database services synced from `routely.yml` and `routely db add` now create internal-only database records, backup job/run DTOs expose local metadata without served download links and only mark succeeded files available, failed backup runs return explicit public error metadata, deploy failure metadata/logs redact known app secrets, Compose/internal health checks report honest unavailable state when no host port is reachable, and health responses include dashboard-ready availability/reason/message summaries. Verified with `npm exec --workspace apps/cli -- vitest run src/checkpoint9.test.ts src/checkpoint10.test.ts src/checkpoint12.test.ts`, `node --check apps/daemon/src/server.js`, `npm run lint`, `npm run test --workspace apps/cli`, and `npm run build --workspace apps/cli`.

- [ ] Backend: run an integration verification pass for backend-owned Compose-first changes and document blockers. Read the current git history from this playbook run, `docs/03-demo-acceptance-plan.md`, and the files changed by previous Backend tasks in this execution folder. Do not broaden scope or rewrite unrelated code. Run the relevant aggregate checks for backend-owned work: `npm run lint`, `npm run test --workspace apps/cli`, `npm run build --workspace apps/cli`, `node --check apps/daemon/src/server.js`, and any targeted package tests added during this run. If all checks pass and there are backend-owned docs or code changes from this task, commit only those files with a concise Backend message. If checks fail, determine whether each failure is caused by the current Backend changes or an unrelated existing issue; fix current-task failures, but report unrelated blockers with exact command output in the Auto Run task output.
