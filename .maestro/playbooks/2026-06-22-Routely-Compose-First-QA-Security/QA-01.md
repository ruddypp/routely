# Routely Compose-First QA E2E Validation

Task-based playbook: each checkbox task must be executable in a fresh QA E2E agent context. QA owns reports under `docs/qa/` and must not commit reports. Report findings to Routely Lead only. Preserve unrelated dirty worktree changes and do not edit production code.

- [ ] QA E2E: validate the completed Compose-first Routely implementation and write an end-to-end QA report. Read `AGENTS.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/02-team-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/09-current-status.md`, `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/BACKEND-01.md`, `.maestro/playbooks/2026-06-22-Routely-Compose-First-Execution/FRONTEND-01.md`, and recent commits from the Compose-first run before testing. Verify the dashboard-first solo-operator flow from docs: app registry/create/edit for Compose-backed apps, Start All starts enabled apps and skips disabled apps, per-app stop does not change enablement, enable/disable remains non-destructive, production operation screens honestly show deploy/domain/proxy/env/secrets/database/backup/log/history/health state, and GitHub redeploy diagnostics expose branch/delivery/deploy/failure/log state without unsupported claims. Run the narrowest available checks first, then broader checks where practical: `npm run lint`, `npm run test --workspace apps/web`, `npm run test --workspace apps/cli`, `npx tsc --noEmit --project apps/web/tsconfig.json`, and any existing dashboard smoke command only if environment prerequisites are available. Create a dated report under `docs/qa/` with YAML front matter, environment details, exact commands, expected/actual results, pass/fail/blocked status, evidence, and concrete Backend/Frontend bug instructions for any blockers. Do not edit production code. Do not commit the QA report.
