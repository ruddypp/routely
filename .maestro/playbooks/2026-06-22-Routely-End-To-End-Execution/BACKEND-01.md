# Backend Phase 01: Runtime And API Contracts

Playbook type: task-based. Each checkbox is self-contained and can run in a fresh agent context.

- [ ] Backend: read `AGENTS.md`, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/05-architecture.md`, `docs/06-interfaces.md`, `docs/07-security-and-risks.md`, `docs/10-implementation-backlog.md`, `docs/11-feature-scope.md`, `docs/13-end-to-end-execution-plan.md`, `packages/core/src/config.js`, `packages/db/src/index.js`, `packages/drivers/src/index.js`, and `apps/cli/src/index.ts`. Harden the one-command local bootstrap and app registry contract for Slice 1 and Slice 2: `routely` should report daemon/dashboard readiness clearly, preserve `enabled`, driver, command/Compose/Dockerfile metadata, dependencies, ports, healthcheck, domain, source, and env metadata, and return actionable errors for daemon startup, port conflict, unsupported driver/preset, and invalid config. Add or update behavior tests at the CLI/package seam. Verification: run targeted tests for changed CLI/package files, `npm run test --workspace apps/cli`, `npm run lint`, and `npm run build --workspace apps/cli`. Commit only Backend-owned files if verification passes.

- [ ] Backend: read `docs/13-end-to-end-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, `apps/daemon/src/server.js`, `packages/db/src/index.js`, `packages/drivers/src/index.js`, `packages/proxy/src/index.js`, and related tests in `apps/cli/src/checkpoint*.test.ts`. Harden Slice 3 and Slice 4 contracts: Start All filters enabled apps, per-app stop is distinct from disable-for-future-starts, logs and health report real or honest unavailable state, production init/doctor reports readiness, and production mutations require auth/admin token. Add or update tests for enabled filtering, stop vs disable semantics, crash/log/health unavailable states, production auth, and doctor/status responses. Verification: run targeted tests, `npm run test --workspace apps/cli`, `node --check apps/daemon/src/server.js`, `npm run lint`, and `npm run build --workspace apps/cli`. Commit only Backend-owned files if verification passes.

- [ ] Backend: read `docs/13-end-to-end-execution-plan.md`, `docs/07-security-and-risks.md`, `apps/daemon/src/server.js`, `packages/github/src/index.js`, `packages/proxy/src/index.js`, `packages/db/src/index.js`, `packages/presets/src/index.js`, and related CLI tests. Harden Slice 5, Slice 6, and Slice 7 contracts: one-VPS deploy state, domain verification, proxy route generation, conservative HTTPS/TLS status, latest successful deployment, deploy phases/logs, GitHub signature validation/dedupe/repo-branch mapping, env/secrets redaction, internal-by-default database services, backup job/run metadata, metrics/health/log/deploy history, and notifications. Do not imply unsupported restore, TLS success, or external backup behavior. Verification: run targeted tests, `npm run test --workspace apps/cli`, `node --check apps/daemon/src/server.js`, `npm run lint`, and `npm run build --workspace apps/cli`. Commit only Backend-owned files if verification passes.
