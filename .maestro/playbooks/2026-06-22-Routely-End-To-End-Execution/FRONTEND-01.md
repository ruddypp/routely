# Frontend Phase 01: Dashboard Implementation

Playbook type: task-based. Each checkbox is self-contained and can run in a fresh agent context.

- [ ] Frontend: read `AGENTS.md`, `CONTEXT.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/06-interfaces.md`, `docs/11-feature-scope.md`, `docs/13-end-to-end-execution-plan.md`, `docs/qa/end-to-end-uiux-dashboard-contract-2026-06-22.md` if it exists, and the relevant Next.js 16 docs under `node_modules/next/dist/docs/` before editing `apps/web`. Inspect `apps/web/src/app/dashboard-client.tsx`, `apps/web/src/components/dashboard/`, `apps/web/src/components/ui/`, `apps/web/src/lib/`, and `apps/web/src/app/api/`. Implement Slice 1 and Slice 2 dashboard behavior: server readiness, app registry create/edit/onboarding, stack preset display, enablement, driver metadata, and honest unsupported/deferred states. Browser code must use same-origin `/api/*` routes only. Add or update frontend API/UI tests for changed behavior. Verification: `npm run test --workspace apps/web`, `npm run lint --workspace apps/web`, and `npm run build --workspace apps/web`. Commit only Frontend-owned files if verification passes.

- [ ] Frontend: read `docs/13-end-to-end-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, UI/UX handoff if present, and relevant Next.js docs under `node_modules/next/dist/docs/` before editing `apps/web`. Implement Slice 3 and Slice 4 dashboard behavior: Start All, per-app stop, disable-for-future-starts, logs, status, health, dependency/crash/port conflict states, local vs production mode/server readiness, production auth-missing state, and production-blocked state. Use real API data and avoid daemon-direct browser calls. Verification: update tests for lifecycle/status behavior, run `npm run test --workspace apps/web`, `npm run lint --workspace apps/web`, `npm run build --workspace apps/web`, and `npm run test:dashboard-smoke --workspace apps/web` if the app can be started in the task environment. Commit only Frontend-owned files if verification passes.

- [ ] Frontend: read `docs/13-end-to-end-execution-plan.md`, `docs/03-demo-acceptance-plan.md`, UI/UX handoff if present, and relevant Next.js docs under `node_modules/next/dist/docs/` before editing `apps/web`. Implement Slice 5, Slice 6, and Slice 7 dashboard behavior where backend contracts exist: deploy phases/history/logs, domain DNS/proxy/HTTPS states, GitHub connection/deliveries/repo-branch mapping/failure phase, env/secrets redacted metadata, databases, backups, metrics, health, notifications, and deferred-state copy. Avoid success colors/copy for generated, pending, unverified, failed, or unavailable states. Verification: update route/UI tests, run `npm run test --workspace apps/web`, `npm run lint --workspace apps/web`, and `npm run build --workspace apps/web`. Commit only Frontend-owned files if verification passes.
