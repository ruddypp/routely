# PM Phase 01: Issue Tracker And Scope Readiness

Playbook type: task-based. Each checkbox is self-contained and can run in a fresh agent context.

- [ ] PM: read `AGENTS.md`, `CONTEXT.md`, `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/10-implementation-backlog.md`, `docs/12-prd.md`, and `docs/13-end-to-end-execution-plan.md`. Verify the GitHub repository `ruddypp/routely` has the canonical triage labels `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. If labels are missing and `gh` is authenticated, create only those missing canonical labels without creating duplicates. Then create one GitHub issue per vertical slice in `docs/13-end-to-end-execution-plan.md`, label each with `ready-for-agent`, and include owner, blocked-by, acceptance criteria, verification, and source doc links. If issue creation is blocked, write issue bodies to `docs/10-implementation-backlog.md` under a new dated section instead. Verification: `gh issue list --repo ruddypp/routely --state open --limit 20` shows the created slice issues or `docs/10-implementation-backlog.md` contains the fallback issue bodies. Commit only PM-owned docs/playbook changes if files changed and verification passes.

- [ ] PM: after UI/UX, Backend, Frontend, QA, or Security report blockers, update `docs/13-end-to-end-execution-plan.md` with any scope clarification that changes acceptance criteria. Do not edit production code. Preserve the three public-alpha demos as the release gate. Verification: docs still point to local demo, one-VPS demo, and GitHub redeploy demo; no unsupported feature is promoted to implemented. Commit only PM-owned docs changes if files changed and verification passes.
