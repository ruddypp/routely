# PM Phase 01: Issue Tracker And Scope Readiness

Playbook type: task-based. Each checkbox is self-contained and can run in a fresh agent context.

- [x] PM: read `AGENTS.md`, `CONTEXT.md`, `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`, `docs/00-product-brief.md`, `docs/01-alpha-plan.md`, `docs/03-demo-acceptance-plan.md`, `docs/10-implementation-backlog.md`, `docs/12-prd.md`, and `docs/13-end-to-end-execution-plan.md`. Verify the GitHub repository `ruddypp/routely` has the canonical triage labels `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. If any required label is missing, do not create issues with substitute labels; write the vertical-slice issue bodies to `docs/10-implementation-backlog.md` under a new dated section and mention which labels need to be created. If all required labels exist and `gh` is authenticated, create one GitHub issue per vertical slice in `docs/13-end-to-end-execution-plan.md`, label each with `ready-for-agent`, and include owner, blocked-by, acceptance criteria, verification, and source doc links. Verification: `gh issue list --repo ruddypp/routely --state open --limit 20` shows the created slice issues or `docs/10-implementation-backlog.md` contains the fallback issue bodies. Commit only PM-owned docs/playbook changes if files changed and verification passes.
  - Completion note (2026-06-22): Read the required PM source docs, confirmed `gh` is authenticated for `ruddypp/routely`, and verified canonical label readiness. GitHub has `wontfix`, but is missing `needs-triage`, `needs-info`, `ready-for-agent`, and `ready-for-human`; no substitute labels were used and no GitHub issues were created. Added the eight fallback vertical-slice issue bodies to `docs/10-implementation-backlog.md` under `2026-06-22 GitHub Issue Fallback: End-To-End Vertical Slices` with owner, blocked-by, acceptance criteria, verification, and source doc links.

- [ ] PM: after UI/UX, Backend, Frontend, QA, or Security report blockers, update `docs/13-end-to-end-execution-plan.md` with any scope clarification that changes acceptance criteria. Do not edit production code. Preserve the three public-alpha demos as the release gate. Verification: docs still point to local demo, one-VPS demo, and GitHub redeploy demo; no unsupported feature is promoted to implemented. Commit only PM-owned docs changes if files changed and verification passes.
  - Notes: not run in this manifest pass because UI/UX, Backend, Frontend, QA, and Security blocker reports are downstream of `PM-01.md` in launch order.
