# Use a Traefik-Compatible Proxy Model

Routely follows a Traefik-compatible proxy model for app domains because Compose labels and dynamic configuration fit the primary runtime model. This lets Routely route public domains to managed services and support HTTPS automation without requiring operators to hand-write Nginx, Caddy, or Traefik configuration.
