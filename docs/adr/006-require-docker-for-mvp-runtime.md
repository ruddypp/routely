# Require Docker for the MVP Runtime

Routely requires Docker and Docker Compose for the MVP runtime because managed apps are represented through a Compose-backed model on whichever machine runs `routely`. This trades broader no-Docker convenience for a more reliable beginner experience with one foundation for domains, databases, logs, monitoring, and lifecycle control.
