# Use Docker as the Managed App Runtime

Routely uses Docker as the managed app runtime for the MVP because the product must run real apps consistently on whichever machine runs `routely`. This gives the runtime host one practical foundation for app lifecycle, databases, domains, logs, and monitoring instead of separate local and VPS execution paths.
