services:
  postgres:
    image: "postgres:16"
    environment:
      POSTGRES_HOST_AUTH_METHOD: "trust"
      POSTGRES_DB: "app"
    volumes:
      - "postgres_data:/var/lib/postgresql/data"
volumes:
  postgres_data: {}
